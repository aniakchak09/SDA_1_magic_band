prima data am creat functiile pentru operatiile de tip update

ele primesc drept parametrii banda si, unde este cazul, caracterul care trebuie inserat sau cautat

apoi am creat structura pentru coada si functiile necesare pentru lucrul cu aceasta coada

in functia "Citire" parcurg pe rand fiecare linie din fisierul de input, identificand tipul de comanda. daca este o operatie de tip update, o adaug in coada, daca este execute, execut operatia de la inceputul cozii si apoi o elimin din coada, iar daca este show sau undo/redo le execut direct

pentru a ma asigura ca nu este golita complet coada cu operatii si ca se pierde adresa acesteia, in cazul in care se ajunge la finalul ei adaug o celula ce are ca rol pastrarea adresei cozii

in functia de executare, verific ce comanda de tip update este executata si o execut

pentru undo si redo am creat o structura de tip stiva, initializand in main cate o stiva pentru fiecare operatie

in functia de undo, introduc operatia in stiva de redo, iar apo verific ce operatie se afla in varful stivei undo si execut operatia opusa acesteia. de exemplu daca a fost executata operatia MOVE_RIGHT si doresc sa ii aplic undo, atunci voi executa MOVE_LEFT

in functia de redo, verific operatia ce a fost executata initial, si apoi o mai aplic in ca o data. de exemplu, daca a fost executat MOVE_RIGHT, iar apoi a fost aplicat undo pe aceasta, pentru a face redo voi executa iarasi MOVE_RIGHT

in final am incercat sa dezaloc memoria cat de bine am putut

